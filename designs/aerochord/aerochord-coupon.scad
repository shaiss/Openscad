// Single-voice test coupon (PRINT THIS FIRST): one complete fipple + resonator
// straight from the production modules (nothing copied), so you can dial in
// flue_h / cutup / tune on a ~30-40 min print before committing to the full
// ~4 h chord. Tune in the steps given in NOTES.md "Print this first", then
// carry the values into the full instrument.
include <aerochord.scad>
chord_ratios = [1];   // just the root voice
