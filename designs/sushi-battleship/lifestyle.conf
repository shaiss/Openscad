# Tier-2 AI lifestyle-shot prompts for scripts/lifestyle-shot.sh — one
# "<shot> | <prompt>" per line (# starts a comment). <shot> should match a
# tier-1 shots.conf entry so the output is previews/lifestyle-<shot>.png.
#
# These are COSMETIC and text-to-image (GLM-Image has no reference input), so
# the generated geometry is only an impression of the real part — that is
# exactly why readme-gate requirement 9 forces the "AI-styled scene" label and
# the "geometry is approximate" caption. Describe the SCENE, not fake detail.
product-hero | A charcoal-gray 3D-printed Battleship-style tabletop game board with a 4x4 grid of small square sliding shutters, sitting on a set dinner table beside plates of fresh sushi rolls, chopsticks on rests, and small soy-sauce dishes, warm izakaya lighting, shallow depth of field, photorealistic product photography, cozy and inviting
